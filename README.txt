
REMINDERS:

* Don't forget to the mysql driver jar to tomcat's lib/ folder.

* You can find the user logins and passwords in the setup-db.sql file.

To run the project, you may need to add Commons FileUpload jar to tomcat's lib/ folder.

And you may also need to set maxSavePostSize for tomcat connectors.